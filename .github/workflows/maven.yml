name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17' # Specify the version of JDK you need

    - name: Build with Maven
      run: mvn clean install

    - name: Run tests
      run: mvn test

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' # Deploy only from the main branch

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17' 

    - name: Build with Maven
      run: mvn clean package

    #- name: Deploy to Server
     # env:
      #  SERVER_USER: ${{ secrets.SERVER_USER }}
       # SERVER_HOST: ${{ secrets.SERVER_HOST }}
        # SERVER_PORT: ${{ secrets.SERVER_PORT }}
         # DEPLOY_DIR: ${{ secrets.DEPLOY_DIR }}
     # run: |
      #  scp -P $SERVER_PORT target/myapp.jar $SERVER_USER@$SERVER_HOST:$DEPLOY_DIR/
       # ssh -p $SERVER_PORT $SERVER_USER@$SERVER_HOST "java -jar $DEPLOY_DIR/myapp.jar"

